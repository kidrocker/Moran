<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:background="@color/colorAccent"
    android:gravity="bottom"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">


        <ImageView
            android:layout_height="70dp"
            android:id="@+id/web_pic"
            android:layout_width="70dp"
            android:padding="0dp"
            android:layout_alignParentTop="true"
            android:background="@drawable/account_back"
            android:src="@mipmap/ic_account">

        </ImageView>
    <ImageView
        android:layout_height="wrap_content"
        android:id="@+id/bt_close"
        android:layout_width="wrap_content"
        android:padding="0dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:background="@drawable/account_back"
        android:src="@mipmap/ic_close">

    </ImageView>


    <TextView
        android:layout_width="match_parent"
        android:id="@+id/dr_username"
        android:layout_height="wrap_content"
        android:layout_below="@+id/web_pic"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        android:text="Hi there Kiura"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1" />

    <TextView
        android:id="@+id/dr_membership"
        android:layout_below="@+id/dr_username"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Basic" />

</RelativeLayout>
